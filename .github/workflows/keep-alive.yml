name: Keep Streamlit app awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app to prevent it from sleeping
        run: curl -sSf --retry 3 --retry-delay 10 https://shinex2.streamlit.app/ > /dev/null
